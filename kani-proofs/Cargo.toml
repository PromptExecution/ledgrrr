[package]
name = "kani-proofs"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "kani_proofs"

[dependencies]
ledger-core = { path = "../crates/ledger-core" }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }
