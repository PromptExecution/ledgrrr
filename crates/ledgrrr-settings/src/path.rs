//! Settings file path utilities.
//! (To be populated by a later task)

use std::path::PathBuf;

/// Return the default settings path (stub - to be populated in later task)
pub fn default_settings_path() -> PathBuf {
    PathBuf::new()
}
