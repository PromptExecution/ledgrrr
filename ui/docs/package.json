{
  "name": "ledgerr-docs-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync-bindings": "node scripts/sync-bindings.mjs",
    "dev": "npm run sync-bindings && vite",
    "build": "npm run sync-bindings && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.0.0",
    "@tauri-apps/api": "^2.9.0"
  }
}
