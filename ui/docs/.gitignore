node_modules/
dist/

# Synced copy of ../../crates/ui/bindings.ts, written by
# `npm run sync-bindings` (see scripts/sync-bindings.mjs). The canonical
# generated file lives at crates/ui/bindings.ts and is regenerated by
# `cargo build -p ledgerr-host --bin host-tauri`; this local copy exists
# only so bare-specifier imports inside it resolve via this project's own
# node_modules, and is rebuilt automatically before dev/build.
src/generated/
