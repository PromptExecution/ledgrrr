[package]
name = "kani-proofs"
version = "1.9.0"
edition = "2021"
publish = false

[lib]
name = "kani_proofs"

[dependencies]
ledger-core = { path = "../crates/ledger-core" }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }
